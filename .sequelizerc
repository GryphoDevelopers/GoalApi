const { resolve } = require('path');
 
module.exports = {
  config: resolve(__dirname, 'src', 'domain', 'context.json'),
  'models-path': resolve(__dirname, 'src', 'domains', 'rules'),
  'migrations-path': resolve(__dirname, 'src', 'migrations'),
  'seeders-path': resolve(__dirname, 'src', 'domains', 'seeds'),
}